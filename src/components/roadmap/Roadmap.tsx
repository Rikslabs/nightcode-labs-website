import { headquartersRepositories } from "@/repositories";
import styles from "./Roadmap.module.css";
const directives = headquartersRepositories.missions.getStrategicDirectives();
export function Roadmap() { return <section id="roadmap" className={`${styles.section} section-space`} aria-labelledby="directives-title"><div className="container-shell"><header className={styles.header}><p className={styles.eyebrow}>FLOOR 03 / MISSION CONTROL</p><h2 id="directives-title">STRATEGIC DIRECTIVES</h2><p>Long-term engineering initiatives that guide the evolution of NightCode Headquarters.</p></header><div className={styles.wall} aria-label="NightCode strategic directives">{directives.map((directive) => <article className={styles.directive} key={directive.id}><div className={styles.directiveTop}><span>{directive.id}</span><i aria-hidden="true" /></div><h3>{directive.title}</h3><p>{directive.objective}</p><span className={styles.status}>{directive.status}</span></article>)}</div></div></section>; }
