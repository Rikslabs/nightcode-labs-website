import { headquartersGuide } from "@/jarvis";

export function JarvisHeadquartersGuide() {
  const guide = headquartersGuide.getWelcomeGuide();
  return <section id="jarvis" aria-labelledby="jarvis-title" className="section-space"><div className="container-shell"><div className="mx-auto max-w-3xl rounded-2xl border border-border-strong bg-surface/40 p-6 shadow-[inset_0_1px_rgb(248_250_252_/_0.05)] sm:p-8"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="mono text-xs uppercase tracking-[0.22em] text-electric-cyan">JARVIS / HEADQUARTERS GUIDE</p><h2 id="jarvis-title" className="mt-3 text-2xl font-medium tracking-[-0.04em] text-pure-white sm:text-3xl">{guide.title}</h2></div>{guide.status && <span className="mono rounded-full border border-electric-cyan/30 px-3 py-1 text-[0.65rem] uppercase tracking-[0.16em] text-electric-cyan">{guide.status}</span>}</div><p className="mt-5 max-w-2xl text-sm leading-7 text-muted-foreground sm:text-base">{guide.message}</p>{guide.actionLabel && guide.targetSectionId && <a href={`#${guide.targetSectionId}`} className="mt-6 inline-flex min-h-10 items-center rounded-md border border-border-strong px-4 text-xs uppercase tracking-[0.14em] text-pure-white transition-colors hover:border-electric-cyan/60 hover:bg-surface focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-electric-cyan">{guide.actionLabel}</a>}</div></div></section>;
}
