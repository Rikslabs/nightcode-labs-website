import type { HeadquartersSnapshot } from "@/snapshots";
export type ResearchHealth = "Active" | "Stable" | "Experimental" | "Planning" | "Attention Required";
export type ResearchStatus = Readonly<{ totalPrograms: number; activePrograms: number; experimentalPrograms: number; researchPrograms: number; plannedPrograms: number; completedPrograms: number; featuredProgram: string; highestPriorityProgram: string; primaryExperiment: string; researchHealth: ResearchHealth; experimentalRatio: number; completionPercentage: number; protocolCount: number; headquartersResearchReadiness: number; generatedAt: string }>;
const count = (snapshot: HeadquartersSnapshot, statuses: readonly string[]) => snapshot.researchPrograms.filter((program) => statuses.includes(program[3])).length;
export function createResearchStatus(snapshot: HeadquartersSnapshot, generatedAt = new Date().toISOString()): ResearchStatus { const total = snapshot.researchPrograms.length; const active = count(snapshot, ["ACTIVE RESEARCH"]); const experimental = count(snapshot, ["PROTOTYPE RESEARCH", "CONCEPT STUDY"]); const research = count(snapshot, ["ACTIVE RESEARCH", "PROTOTYPE RESEARCH"]); const planned = count(snapshot, ["EXPLORATION", "LONG-TERM RESEARCH"]); const completed = count(snapshot, ["COMPLETED", "COMPLETE"]); const health: ResearchHealth = total === 0 ? "Planning" : active === 0 && experimental === 0 ? "Attention Required" : experimental > active ? "Experimental" : active > 0 ? "Active" : "Stable"; const inputs = [total > 0, active > 0 || experimental > 0, snapshot.researchProtocols.length > 0, true, snapshot.researchPrograms.every((program) => program.every(Boolean))]; return Object.freeze({ totalPrograms: total, activePrograms: active, experimentalPrograms: experimental, researchPrograms: research, plannedPrograms: planned, completedPrograms: completed, featuredProgram: "MULTIMODAL JARVIS", highestPriorityProgram: "MULTIMODAL JARVIS", primaryExperiment: "MULTIMODAL JARVIS", researchHealth: health, experimentalRatio: total === 0 ? 0 : Math.round(experimental / total * 100), completionPercentage: total === 0 ? 0 : Math.round(completed / total * 100), protocolCount: snapshot.researchProtocols.length, headquartersResearchReadiness: Math.round(inputs.filter(Boolean).length / inputs.length * 100), generatedAt }); }
